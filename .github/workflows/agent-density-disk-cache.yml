name: Add density disk cache
on:
  push:
    branches:
      - agent/density-meshing
permissions:
  contents: write
jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/density-meshing
          fetch-depth: 0
      - name: Implement persistent density baseline cache
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path

          def replace_once(path, old, new):
              p = Path(path)
              text = p.read_text(encoding='utf-8')
              if new in text:
                  return
              if old not in text:
                  raise RuntimeError(f'missing anchor in {path}: {old[:100]!r}')
              p.write_text(text.replace(old, new, 1), encoding='utf-8')

          store_h = r'''#pragma once

#include "CoreMinimal.h"

class FCubusDensitySamplingBuffer;

/**
 * Identity of one generated density baseline on local disk.
 *
 * Player edits are intentionally excluded: the cached baseline is deterministic
 * generated world state, while sparse edits remain authoritative in persistence
 * and are applied after the baseline is loaded.
 */
struct ORAKAI_API FCubusDensityChunkStoreContext
{
    int64 WorldSeed = 1;
    uint32 GenerationVersion = 1;
    float VoxelSize = 100.0f;
    int32 SubdivisionsPerVoxel = 1;
};

/** Versioned binary storage for canonical density sampling buffers. */
class ORAKAI_API FCubusDensityChunkStore
{
public:
    static constexpr uint32 CurrentFormatVersion = 1;

    static bool SaveBuffer(
        const FCubusDensitySamplingBuffer& Buffer,
        const FCubusDensityChunkStoreContext& Context
    );

    static bool LoadBuffer(
        const FIntVector& ChunkCoordinate,
        const FCubusDensityChunkStoreContext& Context,
        FCubusDensitySamplingBuffer& OutBuffer
    );

    static bool HasBuffer(
        const FIntVector& ChunkCoordinate,
        const FCubusDensityChunkStoreContext& Context
    );

    static bool DeleteBuffer(
        const FIntVector& ChunkCoordinate,
        const FCubusDensityChunkStoreContext& Context
    );

    static FString GetBufferPath(
        const FIntVector& ChunkCoordinate,
        const FCubusDensityChunkStoreContext& Context
    );

    static FString GetDensityStoreDirectory(
        const FCubusDensityChunkStoreContext& Context
    );
};
'''

          store_cpp = r'''#include "CubusCore/Storage/CubusDensityChunkStore.h"

#include "CubusCore/Chunks/CubusDensitySamplingBuffer.h"
#include "CubusCore/Data/CubusDensitySample.h"
#include "CubusCore/Storage/CubusChunkStore.h"

#include "HAL/FileManager.h"
#include "HAL/PlatformTLS.h"
#include "Misc/Crc.h"
#include "Misc/FileHelper.h"
#include "Misc/Paths.h"
#include "Serialization/MemoryReader.h"
#include "Serialization/MemoryWriter.h"

namespace CubusDensityChunkStore
{
    constexpr uint32 Magic = 0x43444E53u; // "CDNS"
    constexpr int32 BytesPerSample = sizeof(float) + sizeof(int32);

    void SerializeHeader(
        FArchive& Archive,
        uint32& InOutMagic,
        uint32& InOutFormatVersion,
        int64& InOutWorldSeed,
        uint32& InOutGenerationVersion,
        FIntVector& InOutChunkCoordinate,
        float& InOutVoxelSize,
        int32& InOutSubdivisionsPerVoxel,
        int32& InOutSampleDimension,
        int32& InOutSampleCount,
        FVector& InOutSampleOffsetInVoxels,
        int32& InOutPayloadSize,
        uint32& InOutPayloadCrc
    )
    {
        Archive << InOutMagic;
        Archive << InOutFormatVersion;
        Archive << InOutWorldSeed;
        Archive << InOutGenerationVersion;
        Archive << InOutChunkCoordinate;
        Archive << InOutVoxelSize;
        Archive << InOutSubdivisionsPerVoxel;
        Archive << InOutSampleDimension;
        Archive << InOutSampleCount;
        Archive << InOutSampleOffsetInVoxels;
        Archive << InOutPayloadSize;
        Archive << InOutPayloadCrc;
    }

    bool IsSupportedContext(const FCubusDensityChunkStoreContext& Context)
    {
        return
            Context.SubdivisionsPerVoxel == 1 &&
            FMath::IsFinite(Context.VoxelSize) &&
            Context.VoxelSize >= 1.0f;
    }
}

bool FCubusDensityChunkStore::SaveBuffer(
    const FCubusDensitySamplingBuffer& Buffer,
    const FCubusDensityChunkStoreContext& Context
)
{
    if (!Buffer.IsBuilt() || !CubusDensityChunkStore::IsSupportedContext(Context))
    {
        return false;
    }

    const TConstArrayView<FCubusDensitySample> Samples = Buffer.GetSamples();
    if (Samples.Num() != FCubusDensitySamplingBuffer::SampleCount)
    {
        return false;
    }

    TArray<uint8> Payload;
    Payload.Reserve(Samples.Num() * CubusDensityChunkStore::BytesPerSample);
    FMemoryWriter PayloadWriter(Payload, true);

    for (const FCubusDensitySample& Sample : Samples)
    {
        if (!FMath::IsFinite(Sample.Density) || Sample.MaterialId < 0)
        {
            return false;
        }

        float Density = Sample.Density;
        int32 MaterialId = Sample.MaterialId;
        PayloadWriter << Density;
        PayloadWriter << MaterialId;
    }
    PayloadWriter.Close();

    uint32 HeaderMagic = CubusDensityChunkStore::Magic;
    uint32 FormatVersion = CurrentFormatVersion;
    int64 WorldSeed = Context.WorldSeed;
    uint32 GenerationVersion = Context.GenerationVersion;
    FIntVector ChunkCoordinate = Buffer.GetChunkCoordinate();
    float VoxelSize = Context.VoxelSize;
    int32 SubdivisionsPerVoxel = Context.SubdivisionsPerVoxel;
    int32 SampleDimension = FCubusDensitySamplingBuffer::SampleDimension;
    int32 SampleCount = Samples.Num();
    FVector SampleOffsetInVoxels = Buffer.GetSampleOffsetInVoxels();
    int32 PayloadSize = Payload.Num();
    uint32 PayloadCrc = FCrc::MemCrc32(Payload.GetData(), Payload.Num());

    TArray<uint8> FileData;
    FMemoryWriter Writer(FileData, true);
    CubusDensityChunkStore::SerializeHeader(
        Writer,
        HeaderMagic,
        FormatVersion,
        WorldSeed,
        GenerationVersion,
        ChunkCoordinate,
        VoxelSize,
        SubdivisionsPerVoxel,
        SampleDimension,
        SampleCount,
        SampleOffsetInVoxels,
        PayloadSize,
        PayloadCrc
    );
    Writer.Serialize(Payload.GetData(), Payload.Num());
    Writer.Close();

    const FString StoreDirectory = GetDensityStoreDirectory(Context);
    IFileManager::Get().MakeDirectory(*StoreDirectory, true);

    const FString FinalPath = GetBufferPath(ChunkCoordinate, Context);
    const FString TemporaryPath = FinalPath + FString::Printf(
        TEXT(".%u.tmp"),
        FPlatformTLS::GetCurrentThreadId()
    );

    if (!FFileHelper::SaveArrayToFile(FileData, *TemporaryPath))
    {
        UE_LOG(LogTemp, Warning, TEXT("Cubus density cache failed temporary write: %s"), *TemporaryPath);
        return false;
    }

    IFileManager::Get().Delete(*FinalPath, false, true, true);
    if (!IFileManager::Get().Move(*FinalPath, *TemporaryPath, true, true, false, true))
    {
        IFileManager::Get().Delete(*TemporaryPath, false, true, true);
        UE_LOG(LogTemp, Warning, TEXT("Cubus density cache failed atomic move: %s"), *FinalPath);
        return false;
    }

    UE_LOG(
        LogTemp,
        Verbose,
        TEXT("Cubus density cache saved (%d, %d, %d): %d samples, %d bytes"),
        ChunkCoordinate.X,
        ChunkCoordinate.Y,
        ChunkCoordinate.Z,
        SampleCount,
        FileData.Num()
    );
    return true;
}

bool FCubusDensityChunkStore::LoadBuffer(
    const FIntVector& ChunkCoordinate,
    const FCubusDensityChunkStoreContext& Context,
    FCubusDensitySamplingBuffer& OutBuffer
)
{
    if (!CubusDensityChunkStore::IsSupportedContext(Context))
    {
        return false;
    }

    const FString Path = GetBufferPath(ChunkCoordinate, Context);
    TArray<uint8> FileData;
    if (!FFileHelper::LoadFileToArray(FileData, *Path))
    {
        return false;
    }

    FMemoryReader Reader(FileData, true);

    uint32 HeaderMagic = 0;
    uint32 FormatVersion = 0;
    int64 WorldSeed = 0;
    uint32 GenerationVersion = 0;
    FIntVector StoredCoordinate = FIntVector::ZeroValue;
    float StoredVoxelSize = 0.0f;
    int32 StoredSubdivisions = 0;
    int32 StoredSampleDimension = 0;
    int32 StoredSampleCount = 0;
    FVector StoredSampleOffset = FVector::ZeroVector;
    int32 PayloadSize = 0;
    uint32 StoredPayloadCrc = 0;

    CubusDensityChunkStore::SerializeHeader(
        Reader,
        HeaderMagic,
        FormatVersion,
        WorldSeed,
        GenerationVersion,
        StoredCoordinate,
        StoredVoxelSize,
        StoredSubdivisions,
        StoredSampleDimension,
        StoredSampleCount,
        StoredSampleOffset,
        PayloadSize,
        StoredPayloadCrc
    );

    const int64 PayloadOffset = Reader.Tell();
    const int64 RemainingBytes = Reader.TotalSize() - PayloadOffset;
    const bool bHeaderValid =
        !Reader.IsError() &&
        HeaderMagic == CubusDensityChunkStore::Magic &&
        FormatVersion == CurrentFormatVersion &&
        WorldSeed == Context.WorldSeed &&
        GenerationVersion == Context.GenerationVersion &&
        StoredCoordinate == ChunkCoordinate &&
        FMath::IsNearlyEqual(StoredVoxelSize, Context.VoxelSize, 0.001f) &&
        StoredSubdivisions == Context.SubdivisionsPerVoxel &&
        StoredSampleDimension == FCubusDensitySamplingBuffer::SampleDimension &&
        StoredSampleCount == FCubusDensitySamplingBuffer::SampleCount &&
        !StoredSampleOffset.ContainsNaN() &&
        PayloadSize == StoredSampleCount * CubusDensityChunkStore::BytesPerSample &&
        RemainingBytes == PayloadSize;

    if (!bHeaderValid)
    {
        UE_LOG(LogTemp, Warning, TEXT("Cubus density cache rejected stale or invalid baseline: %s"), *Path);
        return false;
    }

    const uint8* PayloadData = FileData.GetData() + PayloadOffset;
    if (FCrc::MemCrc32(PayloadData, PayloadSize) != StoredPayloadCrc)
    {
        UE_LOG(LogTemp, Warning, TEXT("Cubus density cache rejected corrupt baseline: %s"), *Path);
        return false;
    }

    TArray<FCubusDensitySample> Samples;
    Samples.SetNumUninitialized(StoredSampleCount);

    for (FCubusDensitySample& Sample : Samples)
    {
        Reader << Sample.Density;
        Reader << Sample.MaterialId;
        if (Reader.IsError() || !FMath::IsFinite(Sample.Density) || Sample.MaterialId < 0)
        {
            return false;
        }
    }
    Reader.Close();

    if (!OutBuffer.RestoreGeneratedBaseline(StoredCoordinate, StoredSampleOffset, MoveTemp(Samples)))
    {
        return false;
    }

    UE_LOG(
        LogTemp,
        Verbose,
        TEXT("Cubus density cache hit (%d, %d, %d), seed %lld, generation %u"),
        ChunkCoordinate.X,
        ChunkCoordinate.Y,
        ChunkCoordinate.Z,
        static_cast<long long>(Context.WorldSeed),
        Context.GenerationVersion
    );
    return true;
}

bool FCubusDensityChunkStore::HasBuffer(
    const FIntVector& ChunkCoordinate,
    const FCubusDensityChunkStoreContext& Context
)
{
    return IFileManager::Get().FileExists(*GetBufferPath(ChunkCoordinate, Context));
}

bool FCubusDensityChunkStore::DeleteBuffer(
    const FIntVector& ChunkCoordinate,
    const FCubusDensityChunkStoreContext& Context
)
{
    return IFileManager::Get().Delete(*GetBufferPath(ChunkCoordinate, Context), false, true, true);
}

FString FCubusDensityChunkStore::GetBufferPath(
    const FIntVector& ChunkCoordinate,
    const FCubusDensityChunkStoreContext& Context
)
{
    return FPaths::Combine(
        GetDensityStoreDirectory(Context),
        FString::Printf(
            TEXT("chunk_%d_%d_%d.cubusd"),
            ChunkCoordinate.X,
            ChunkCoordinate.Y,
            ChunkCoordinate.Z
        )
    );
}

FString FCubusDensityChunkStore::GetDensityStoreDirectory(
    const FCubusDensityChunkStoreContext& Context
)
{
    FCubusChunkStoreContext BaseContext;
    BaseContext.WorldSeed = Context.WorldSeed;
    BaseContext.GenerationVersion = Context.GenerationVersion;

    const int32 VoxelSizeMilliCentimetres = FMath::RoundToInt(Context.VoxelSize * 1000.0f);
    return FPaths::Combine(
        FCubusChunkStore::GetWorldStoreDirectory(BaseContext),
        TEXT("Density"),
        FString::Printf(
            TEXT("s%d_v%d"),
            Context.SubdivisionsPerVoxel,
            VoxelSizeMilliCentimetres
        )
    );
}
'''

          Path('Source/Orakai/CubusCore/Storage/CubusDensityChunkStore.h').write_text(store_h, encoding='utf-8')
          Path('Source/Orakai/CubusCore/Storage/CubusDensityChunkStore.cpp').write_text(store_cpp, encoding='utf-8')

          # Sampling buffer exposes immutable samples and a validated restore path.
          replace_once(
              'Source/Orakai/CubusCore/Chunks/CubusDensitySamplingBuffer.h',
              '''    bool IsBuilt() const\n    {\n        return Samples.Num() == SampleCount;\n    }\n\n    const FIntVector& GetChunkCoordinate() const\n''',
              '''    bool IsBuilt() const\n    {\n        return Samples.Num() == SampleCount;\n    }\n\n    TConstArrayView<FCubusDensitySample> GetSamples() const\n    {\n        return MakeArrayView(Samples);\n    }\n\n    /** Restores one validated, edit-free generated baseline from local disk. */\n    bool RestoreGeneratedBaseline(\n        const FIntVector& InChunkCoordinate,\n        const FVector& InSampleOffsetInVoxels,\n        TArray<FCubusDensitySample>&& InSamples\n    );\n\n    const FIntVector& GetChunkCoordinate() const\n'''
          )

          replace_once(
              'Source/Orakai/CubusCore/Chunks/CubusDensitySamplingBuffer.cpp',
              '''void FCubusDensitySamplingBuffer::Reset()\n{\n    ChunkCoordinate = FIntVector::ZeroValue;\n    SampleOffsetInVoxels = FVector::ZeroVector;\n    Samples.Reset();\n}\n\nvoid FCubusDensitySamplingBuffer::ApplyEdits(\n''',
              '''void FCubusDensitySamplingBuffer::Reset()\n{\n    ChunkCoordinate = FIntVector::ZeroValue;\n    SampleOffsetInVoxels = FVector::ZeroVector;\n    Samples.Reset();\n}\n\nbool FCubusDensitySamplingBuffer::RestoreGeneratedBaseline(\n    const FIntVector& InChunkCoordinate,\n    const FVector& InSampleOffsetInVoxels,\n    TArray<FCubusDensitySample>&& InSamples\n)\n{\n    if (InSamples.Num() != SampleCount || InSampleOffsetInVoxels.ContainsNaN())\n    {\n        return false;\n    }\n\n    for (const FCubusDensitySample& Sample : InSamples)\n    {\n        if (!FMath::IsFinite(Sample.Density) || Sample.MaterialId < 0)\n        {\n            return false;\n        }\n    }\n\n    ChunkCoordinate = InChunkCoordinate;\n    SampleOffsetInVoxels = InSampleOffsetInVoxels;\n    Samples = MoveTemp(InSamples);\n    return true;\n}\n\nvoid FCubusDensitySamplingBuffer::ApplyEdits(\n'''
          )

          # Build input carries disk-cache identity to both sync and async workers.
          replace_once(
              'Source/Orakai/CubusCore/Actors/CubusVoxelVolumeActor.h',
              '''\tfloat VoxelSize\t\t\t   = 100.0f;\n\tint32 SubdivisionsPerVoxel = 1;\n\tfloat IsoLevel\t\t\t   = 0.0f;\n\n\tbool bHasGeneratedDensityBuffer = false;\n''',
              '''\tfloat VoxelSize\t\t\t   = 100.0f;\n\tint32 SubdivisionsPerVoxel = 1;\n\tfloat IsoLevel\t\t\t   = 0.0f;\n\n\tint64 WorldSeed = 1;\n\tuint32 GenerationVersion = FCubusGenerationSeeds::CurrentGenerationVersion;\n\tbool bUseDiskDensityCache = true;\n\tbool bHasGeneratedDensityBuffer = false;\n'''
          )

          replace_once(
              'Source/Orakai/CubusCore/Actors/CubusVoxelVolumeActor.cpp',
              '''#include "CubusCore/Meshing/CubusMeshData.h"\n\n#include "HAL/IConsoleManager.h"\n''',
              '''#include "CubusCore/Meshing/CubusMeshData.h"\n#include "CubusCore/Storage/CubusDensityChunkStore.h"\n\n#include "HAL/IConsoleManager.h"\n'''
          )

          replace_once(
              'Source/Orakai/CubusCore/Actors/CubusVoxelVolumeActor.cpp',
              '''\tInput.IsoLevel = 0.0f;\n\n\tif (bHasCachedGeneratedDensityBuffer && CachedGeneratedDensityBuffer.IsValid() && CachedGeneratedDensityBuffer->IsBuilt())\n''',
              '''\tInput.IsoLevel = 0.0f;\n\n\tif (IsValid(OwningBlockWorld.Get()))\n\t{\n\t\tInput.WorldSeed = OwningBlockWorld->GetWorldSeed();\n\t}\n\telse\n\t{\n\t\tInput.WorldSeed = ChunkData->GetGenerationSeeds().World;\n\t}\n\tInput.GenerationVersion = FCubusGenerationSeeds::CurrentGenerationVersion;\n\n\tif (bHasCachedGeneratedDensityBuffer && CachedGeneratedDensityBuffer.IsValid() && CachedGeneratedDensityBuffer->IsBuilt())\n'''
          )

          old_density_branch = '''\tif (Subdivisions <= 1)\n\t{\n\t\tFCubusDensitySamplingBuffer DensityBuffer;\n\n\t\tif (Input.bHasGeneratedDensityBuffer && Input.GeneratedDensityBuffer.IsValid() && Input.GeneratedDensityBuffer->IsBuilt())\n\t\t{\n\t\t\t/*\n\t\t\t * One intentional copy remains here.\n\t\t\t *\n\t\t\t * This worker-local buffer is the only copy that receives player edits.\n\t\t\t * The shared generated baseline stays permanently immutable.\n\t\t\t */\n\t\t\tDensityBuffer = *Input.GeneratedDensityBuffer;\n\t\t}\n\t\telse\n\t\t{\n\t\t\t/*\n\t\t\t * First build for this chunk/configuration.\n\t\t\t *\n\t\t\t * Generate the canonical baseline once and return it to the actor so\n\t\t\t * subsequent edit transactions can reuse it.\n\t\t\t */\n\t\t\tDensityBuffer.Build(Input.ChunkCoordinate, DensityField);\n\n\t\t\tResult.GeneratedDensityBuffer = DensityBuffer;\n\n\t\t\tResult.bHasGeneratedDensityBuffer = true;\n\t\t}\n'''
          new_density_branch = '''\tif (Subdivisions <= 1)\n\t{\n\t\tFCubusDensitySamplingBuffer DensityBuffer;\n\n\t\tif (Input.bHasGeneratedDensityBuffer && Input.GeneratedDensityBuffer.IsValid() && Input.GeneratedDensityBuffer->IsBuilt())\n\t\t{\n\t\t\t/*\n\t\t\t * One intentional copy remains here.\n\t\t\t *\n\t\t\t * This worker-local buffer is the only copy that receives player edits.\n\t\t\t * The shared generated baseline stays permanently immutable.\n\t\t\t */\n\t\t\tDensityBuffer = *Input.GeneratedDensityBuffer;\n\t\t}\n\t\telse\n\t\t{\n\t\t\tFCubusDensityChunkStoreContext CacheContext;\n\t\t\tCacheContext.WorldSeed = Input.WorldSeed;\n\t\t\tCacheContext.GenerationVersion = Input.GenerationVersion;\n\t\t\tCacheContext.VoxelSize = Input.VoxelSize;\n\t\t\tCacheContext.SubdivisionsPerVoxel = Subdivisions;\n\n\t\t\tconst bool bLoadedDiskBaseline =\n\t\t\t\tInput.bUseDiskDensityCache &&\n\t\t\t\tFCubusDensityChunkStore::LoadBuffer(Input.ChunkCoordinate, CacheContext, DensityBuffer);\n\n\t\t\tif (!bLoadedDiskBaseline)\n\t\t\t{\n\t\t\t\t/* Generate the expensive edit-free terrain baseline exactly once. */\n\t\t\t\tDensityBuffer.Build(Input.ChunkCoordinate, DensityField);\n\n\t\t\t\t/* Streaming workers write unique chunk files, keeping disk I/O off the game thread. */\n\t\t\t\tif (Input.bUseDiskDensityCache)\n\t\t\t\t{\n\t\t\t\t\tFCubusDensityChunkStore::SaveBuffer(DensityBuffer, CacheContext);\n\t\t\t\t}\n\t\t\t}\n\n\t\t\t/*\n\t\t\t * Whether generated or loaded, return the immutable baseline to the actor.\n\t\t\t * Later density edits copy and mutate this baseline only in worker-local memory.\n\t\t\t */\n\t\t\tResult.GeneratedDensityBuffer = DensityBuffer;\n\t\t\tResult.bHasGeneratedDensityBuffer = true;\n\t\t}\n'''
          replace_once('Source/Orakai/CubusCore/Actors/CubusVoxelVolumeActor.cpp', old_density_branch, new_density_branch)

          # Explicit cache API now supports density too; useful for fixed/editor chunks and diagnostics.
          replace_once(
              'Source/Orakai/CubusCore/Storage/CubusVoxelVolumeActorStorage.cpp',
              '''#include "CubusCore/Storage/CubusChunkStore.h"\n#include "CubusCore/Generation/CubusTerrainDensityField.h"\n''',
              '''#include "CubusCore/Storage/CubusChunkStore.h"\n#include "CubusCore/Storage/CubusDensityChunkStore.h"\n#include "CubusCore/Generation/CubusTerrainDensityField.h"\n'''
          )

          replace_once(
              'Source/Orakai/CubusCore/Storage/CubusVoxelVolumeActorStorage.cpp',
              '''FCubusChunkStoreContext MakeContext(const FCubusBlockChunkData& ChunkData)\n{\n\tFCubusChunkStoreContext Context;\n\tContext.WorldSeed\t\t  = ChunkData.GetGenerationSeeds().World;\n\tContext.GenerationVersion = FCubusGenerationSeeds::CurrentGenerationVersion;\n\treturn Context;\n}\n''',
              '''FCubusChunkStoreContext MakeContext(const FCubusBlockChunkData& ChunkData)\n{\n\tFCubusChunkStoreContext Context;\n\tContext.WorldSeed\t\t  = ChunkData.GetGenerationSeeds().World;\n\tContext.GenerationVersion = FCubusGenerationSeeds::CurrentGenerationVersion;\n\treturn Context;\n}\n\nFCubusDensityChunkStoreContext MakeDensityContext(const ACubusVoxelVolumeActor& Actor, const FCubusBlockChunkData& ChunkData)\n{\n\tFCubusDensityChunkStoreContext Context;\n\tconst ACubusBlockWorldActor* BlockWorld = Actor.GetOwningBlockWorld();\n\tContext.WorldSeed = IsValid(BlockWorld) ? BlockWorld->GetWorldSeed() : ChunkData.GetGenerationSeeds().World;\n\tContext.GenerationVersion = FCubusGenerationSeeds::CurrentGenerationVersion;\n\tContext.VoxelSize = Actor.GetVoxelSize();\n\tContext.SubdivisionsPerVoxel = FCubusDensityLod::NormalizeSubdivisions(Actor.GetDensitySubdivisionsPerVoxel());\n\treturn Context;\n}\n'''
          )

          replace_once(
              'Source/Orakai/CubusCore/Storage/CubusVoxelVolumeActorStorage.cpp',
              '''bool ACubusVoxelVolumeActor::TryLoadCachedChunk()\n{\n\tif (GetEffectiveRenderMode() == ECubusVoxelRenderMode::Density)\n\t{\n\t\treturn false;\n\t}\n\n\tEnsureChunkData();\n''',
              '''bool ACubusVoxelVolumeActor::TryLoadCachedChunk()\n{\n\tEnsureChunkData();\n\n\tif (GetEffectiveRenderMode() == ECubusVoxelRenderMode::Density)\n\t{\n\t\tFCubusDensitySamplingBuffer LoadedBuffer;\n\t\tconst FCubusDensityChunkStoreContext Context = CubusVoxelVolumeActorStorage::MakeDensityContext(*this, *ChunkData);\n\n\t\tif (!FCubusDensityChunkStore::LoadBuffer(ChunkCoordinate, Context, LoadedBuffer))\n\t\t{\n\t\t\treturn false;\n\t\t}\n\n\t\tCachedGeneratedDensityBuffer =\n\t\t\tMakeShared<FCubusDensitySamplingBuffer, ESPMode::ThreadSafe>(MoveTemp(LoadedBuffer));\n\t\tbHasCachedGeneratedDensityBuffer = true;\n\t\tbChunkCacheDirty = false;\n\t\treturn true;\n\t}\n'''
          )

          replace_once(
              'Source/Orakai/CubusCore/Storage/CubusVoxelVolumeActorStorage.cpp',
              '''bool ACubusVoxelVolumeActor::SaveCachedChunk() const\n{\n\tif (GetEffectiveRenderMode() == ECubusVoxelRenderMode::Density)\n\t{\n\t\treturn true;\n\t}\n\n\tif (!ChunkData.IsValid())\n''',
              '''bool ACubusVoxelVolumeActor::SaveCachedChunk() const\n{\n\tif (!ChunkData.IsValid())\n'''
          )

          replace_once(
              'Source/Orakai/CubusCore/Storage/CubusVoxelVolumeActorStorage.cpp',
              '''\tif (!bChunkCacheDirty)\n\t{\n\t\treturn true;\n\t}\n\n\tconst FCubusChunkStoreContext Context = CubusVoxelVolumeActorStorage::MakeContext(*ChunkData);\n''',
              '''\tif (GetEffectiveRenderMode() == ECubusVoxelRenderMode::Density)\n\t{\n\t\tif (!bHasCachedGeneratedDensityBuffer || !CachedGeneratedDensityBuffer.IsValid() || !CachedGeneratedDensityBuffer->IsBuilt())\n\t\t{\n\t\t\treturn true;\n\t\t}\n\n\t\tconst FCubusDensityChunkStoreContext DensityContext = CubusVoxelVolumeActorStorage::MakeDensityContext(*this, *ChunkData);\n\t\treturn FCubusDensityChunkStore::SaveBuffer(*CachedGeneratedDensityBuffer, DensityContext);\n\t}\n\n\tif (!bChunkCacheDirty)\n\t{\n\t\treturn true;\n\t}\n\n\tconst FCubusChunkStoreContext Context = CubusVoxelVolumeActorStorage::MakeContext(*ChunkData);\n'''
          )

          # Dedicated disk-format regression test.
          test_cpp = r'''#if WITH_DEV_AUTOMATION_TESTS

#include "Misc/AutomationTest.h"

#include "CubusCore/Chunks/CubusDensitySamplingBuffer.h"
#include "CubusCore/Generation/CubusDensityField.h"
#include "CubusCore/Storage/CubusDensityChunkStore.h"

namespace
{
    class FCubusDensityCacheTestField final : public ICubusDensityField
    {
    public:
        virtual FCubusDensitySample Sample(const FIntVector& Coordinate) const override
        {
            FCubusDensitySample Sample;
            Sample.Density = static_cast<float>(Coordinate.Z) - 17.25f + static_cast<float>(Coordinate.X & 3) * 0.03125f;
            Sample.MaterialId = ((Coordinate.X + Coordinate.Y + Coordinate.Z) & 1) == 0 ? 3 : 7;
            return Sample;
        }

        virtual FVector GetSampleOffsetInVoxels() const override
        {
            return FVector(0.25, 0.50, 0.75);
        }
    };
}

IMPLEMENT_SIMPLE_AUTOMATION_TEST(
    FCubusDensityChunkStoreRoundTripTest,
    "Orakai.Cubus.Storage.DensityChunkRoundTrip",
    EAutomationTestFlags::EditorContext | EAutomationTestFlags::EngineFilter
)

bool FCubusDensityChunkStoreRoundTripTest::RunTest(const FString& Parameters)
{
    (void)Parameters;

    const FIntVector Coordinate(21791, -19007, 413);
    FCubusDensityChunkStoreContext Context;
    Context.WorldSeed = 0x123456789LL;
    Context.GenerationVersion = 0x7ffffff0u;
    Context.VoxelSize = 100.0f;
    Context.SubdivisionsPerVoxel = 1;

    FCubusDensityChunkStore::DeleteBuffer(Coordinate, Context);

    FCubusDensityCacheTestField Field;
    FCubusDensitySamplingBuffer Original;
    Original.Build(Coordinate, Field);

    TestTrue(TEXT("Density baseline saves to disk"), FCubusDensityChunkStore::SaveBuffer(Original, Context));
    TestTrue(TEXT("Density baseline file exists"), FCubusDensityChunkStore::HasBuffer(Coordinate, Context));

    FCubusDensitySamplingBuffer Loaded;
    TestTrue(TEXT("Density baseline loads from disk"), FCubusDensityChunkStore::LoadBuffer(Coordinate, Context, Loaded));
    TestTrue(TEXT("Loaded density baseline is built"), Loaded.IsBuilt());
    TestEqual(TEXT("Loaded density baseline keeps its chunk coordinate"), Loaded.GetChunkCoordinate(), Coordinate);
    TestTrue(TEXT("Loaded density baseline keeps sample offset"), Loaded.GetSampleOffsetInVoxels().Equals(Original.GetSampleOffsetInVoxels(), KINDA_SMALL_NUMBER));

    const TConstArrayView<FCubusDensitySample> OriginalSamples = Original.GetSamples();
    const TConstArrayView<FCubusDensitySample> LoadedSamples = Loaded.GetSamples();
    TestEqual(TEXT("Loaded density baseline keeps sample count"), LoadedSamples.Num(), OriginalSamples.Num());

    bool bSamplesMatch = LoadedSamples.Num() == OriginalSamples.Num();
    for (int32 Index = 0; bSamplesMatch && Index < OriginalSamples.Num(); ++Index)
    {
        bSamplesMatch =
            OriginalSamples[Index].Density == LoadedSamples[Index].Density &&
            OriginalSamples[Index].MaterialId == LoadedSamples[Index].MaterialId;
    }
    TestTrue(TEXT("Density baseline round-trip is exact"), bSamplesMatch);

    FCubusDensityChunkStoreContext WrongVoxelSize = Context;
    WrongVoxelSize.VoxelSize = 50.0f;
    FCubusDensitySamplingBuffer WrongLoad;
    TestFalse(TEXT("Different density resolution cannot reuse the cache"),
        FCubusDensityChunkStore::LoadBuffer(Coordinate, WrongVoxelSize, WrongLoad));

    TestTrue(TEXT("Density baseline cache can be deleted"), FCubusDensityChunkStore::DeleteBuffer(Coordinate, Context));
    return true;
}

#endif
'''
          Path('Source/Orakai/CubusCore/Tests/CubusDensityChunkStoreTests.cpp').write_text(test_cpp, encoding='utf-8')
          PY

          git diff --check
          grep -n 'RestoreGeneratedBaseline' Source/Orakai/CubusCore/Chunks/CubusDensitySamplingBuffer.h
          grep -n 'Cubus density cache saved' Source/Orakai/CubusCore/Storage/CubusDensityChunkStore.cpp
          grep -n 'FCubusDensityChunkStore::LoadBuffer' Source/Orakai/CubusCore/Actors/CubusVoxelVolumeActor.cpp
          grep -n 'bUseDiskDensityCache' Source/Orakai/CubusCore/Actors/CubusVoxelVolumeActor.h
          grep -n 'GetEffectiveRenderMode() == ECubusVoxelRenderMode::Density' Source/Orakai/CubusCore/Storage/CubusVoxelVolumeActorStorage.cpp
          grep -n 'DensityChunkRoundTrip' Source/Orakai/CubusCore/Tests/CubusDensityChunkStoreTests.cpp

          python3 - <<'PY'
          from pathlib import Path
          store = Path('Source/Orakai/CubusCore/Storage/CubusDensityChunkStore.cpp').read_text(encoding='utf-8')
          actor = Path('Source/Orakai/CubusCore/Actors/CubusVoxelVolumeActor.cpp').read_text(encoding='utf-8')
          storage = Path('Source/Orakai/CubusCore/Storage/CubusVoxelVolumeActorStorage.cpp').read_text(encoding='utf-8')
          assert 'TEXT("Density")' in store
          assert 'chunk_%d_%d_%d.cubusd' in store
          assert 'StoredPayloadCrc' in store and 'MemCrc32' in store
          assert 'Input.bUseDiskDensityCache' in actor
          assert actor.index('FCubusDensityChunkStore::LoadBuffer') < actor.index('DensityBuffer.Build(Input.ChunkCoordinate, DensityField)')
          assert 'DensityBuffer.ApplyEdits(Input.DensityEdits);' in actor
          assert actor.index('Result.GeneratedDensityBuffer = DensityBuffer;') < actor.index('DensityBuffer.ApplyEdits(Input.DensityEdits);')
          assert 'return false;' not in storage[storage.index('bool ACubusVoxelVolumeActor::TryLoadCachedChunk()'):storage.index('bool ACubusVoxelVolumeActor::SaveCachedChunk() const')].split('ECubusVoxelRenderMode::Density', 1)[0]
          PY

          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add \
            Source/Orakai/CubusCore/Storage/CubusDensityChunkStore.h \
            Source/Orakai/CubusCore/Storage/CubusDensityChunkStore.cpp \
            Source/Orakai/CubusCore/Chunks/CubusDensitySamplingBuffer.h \
            Source/Orakai/CubusCore/Chunks/CubusDensitySamplingBuffer.cpp \
            Source/Orakai/CubusCore/Actors/CubusVoxelVolumeActor.h \
            Source/Orakai/CubusCore/Actors/CubusVoxelVolumeActor.cpp \
            Source/Orakai/CubusCore/Storage/CubusVoxelVolumeActorStorage.cpp \
            Source/Orakai/CubusCore/Tests/CubusDensityChunkStoreTests.cpp
          git commit -m 'Persist generated density chunk baselines'
          git push origin HEAD:agent/density-meshing
